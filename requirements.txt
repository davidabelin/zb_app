Werkzeug>=2.0.3  # Ensure compatibility with Flask
Flask>=2.3.2
flask_session
flask_login
gunicorn>=20.1.0
# NOTE: do not depend on the `google` PyPI package; it can conflict with
# `google-cloud-*` namespace packages.
google-cloud-storage>=2.9.0
google-cloud-firestore
requests>=2.28.2
python-dotenv
openai>=1.61.0
grpcio-tools>=1.76.0
grpcio>=1.76.0
# grpcio-status currently requires protobuf <6 to function correctly
protobuf>=5.26.1,<6
pyyaml
black>=23.9.1
flake8>=6.0.0
mypy>=1.5.1
pre-commit>=3.4.0
pytest
# Pydantic v2 uses a compiled `pydantic-core` wheel; pinning `pydantic-core` too tightly
# breaks on newer Python versions (e.g. 3.14). Let `pydantic` pull the compatible core.
pydantic>=2.11.0
