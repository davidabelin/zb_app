# Runtime dependencies only. Keep deploy-time packages here and move lint/test
# tooling to requirements-dev.txt so Cloud Build does not install unnecessary
# developer extras.
Werkzeug>=2.0.3  # Ensure compatibility with Flask
Flask>=2.3.2
gunicorn>=20.1.0
# NOTE: do not depend on the `google` PyPI package; it can conflict with
# `google-cloud-*` namespace packages.
google-cloud-storage>=2.9.0
google-cloud-firestore
google-cloud-secret-manager
requests>=2.28.2
python-dotenv
openai==1.61.0
pyyaml
# Keep versions explicit to avoid deep dependency backtracking in Cloud Build.
pydantic==2.12.4
